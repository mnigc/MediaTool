# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Rust build artifacts
src-tauri/target

# FFmpeg sidecar binaries (downloaded via scripts/fetch-ffmpeg.ps1, not committed)
src-tauri/binaries/*

# Verification artifacts
scripts/verify.log

# Test-run outputs dropped next to the crate that produced them
clip_mediatool*.mp4
movie_mediatool*.opus
hdr_test.mkv

# Workspace build artifacts
/target

# NAS deployment config: holds MEDIATOOL_TOKEN, never commit
docker/.env
