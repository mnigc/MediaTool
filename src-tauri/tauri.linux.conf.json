{
  "bundle": {
    "targets": ["deb", "rpm", "appimage"],
    "resources": {
      "binaries/ffmpeg": "binaries/",
      "binaries/ffprobe": "binaries/",
      "binaries/yt-dlp": "binaries/"
    }
  }
}
